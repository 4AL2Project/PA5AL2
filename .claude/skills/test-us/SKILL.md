---
name: test-visualy-and-correct-us
description: Test visuellement et corrige l'implémentation d'une US
---

Utilise `playwright` pour tester VISUELLEMENT (en desktop (>1024px) et mobile(<440px)), valider et corriger tous les tests du fichier `tests-book/tests-book-$ARGUMENTS.md`. 

Créé un fichier dans `tests-book` nomé `tests-book-$ARGUMENTS.{{dd-mm-yyyy-hh-mn}}.md` qui reprend les tests en précisant qui est OK et KO.

En suite implémente chaque correction.

Le front et l'api sont déjà lancé donc tu n'a pas besoin de le faire.